{
    "name": "Nextflow",
    "id": "nextflow",
    "version": "1.1.0",
    "description": "Installs Nextflow along with runtime dependencies for data-driven computational pipeline orchestration.",
    "options": {
        "version": {
            "type": "string",
            "proposals": ["latest"],
            "default": "latest",
            "description": "Select the Nextflow version to install (e.g., 24.04.4)."
        }
    },
    "customizations": {},
    "installsAfter": [
        "ghcr.io/devcontainers/features/java"
    ]
}